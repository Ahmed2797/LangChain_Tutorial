#sudo apt update
sudo apt install python3.12-venv -y

# create_env
python3.12 -m venv lc
source lc/bin/activate
conda activate lc


# command_prompt :
sudo apt install wine -y
wine cmd

# pip
pip install --upgrade pip
pip install langchain langchain-core langchain-community langchain-huggingface transformers
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu


cd ~/langchain_class



echo $HUGGINGFACEHUB_API_TOKEN

